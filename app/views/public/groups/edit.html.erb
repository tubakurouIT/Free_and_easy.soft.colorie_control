<div class="container my-5">
  <!-- Group Editing Section -->
  <div class="card shadow-sm">
    <div class="card-body">
      <h2 class="text-primary mb-4">Editing Group</h2>
      
      <!-- Error Messages -->
      <div class="mb-3">
        <%= render 'layouts/errors', obj: @group %>
      </div>

      <!-- Group Editing Form -->
      <%= render 'form', group: @group %>
    </div>
  </div>
</div>
